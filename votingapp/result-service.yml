apiVersion: v1
kind: Service
metatadata:
  name: result-service
  labels:
    name: result-service
    app: kavvotingapp
spec:
  type: LoadBalancer
  ports:
    - port: 80
      tagertPort: 80
  selector:
    name: result-app
    app: kavvotingapp